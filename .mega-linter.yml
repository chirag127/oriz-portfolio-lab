# MegaLinter configuration for portfolio-lab
# Per-linter DISABLE_ERRORS go in the workflow env vars (not here — schema doesn't allow them).
# This file handles filter patterns and threshold overrides.

YAML_YAMLLINT_FILTER_REGEX_EXCLUDE: "(pnpm-lock\\.yaml)"
