---
/*
 * BaseLayout — per-app shell for oriz-portfolio-lab.
 *
 * Head: meta/og/fonts/JsonLd + CF analytics.
 * Body: FamilyChrome, Header, Sidebar (AppSidebarContent), main, Footer,
 * BottomBar, ConsentBanner.
 *
 * Identity: "The Optics Bench" — cool porcelain light-table, indigo capital-
 * market beam refracting through the tangent portfolio, oxide-red plotted
 * assets. No adsense (free public tool).
 * Fonts: Bricolage Grotesque (display) + Spline Sans (body) + Martian Mono (data).
 */
import '@chirag127/astro-shell/tokens.css'
import '~/styles/global.css'
import '~/styles/oriz-ui-overrides.css'
import FamilyChrome from '@chirag127/astro-shell/components/FamilyChrome.astro'
import BottomBar from '@chirag127/astro-chrome/BottomBar.astro'
import ConsentBanner from '@chirag127/astro-chrome/ConsentBanner.astro'
import Footer from '@chirag127/astro-chrome/Footer.astro'
import Sidebar from '@chirag127/astro-chrome/Sidebar.astro'
import AppSidebarContent from '~/components/AppSidebarContent.astro'
import Header from '~/components/Header.astro'
import JsonLd from '~/components/JsonLd.astro'
import { bottomBarActions } from '~/data/bottombar-actions'
import { SITE_CONFIG } from '~/lib/siteConfig'

interface Props {
  title?: string
  description?: string
  canonical?: string
  noindex?: boolean
  /** Page-level JSON-LD blocks appended to the default Org+WebSite+Person triple. */
  jsonLd?: Record<string, unknown> | Record<string, unknown>[]
}

const {
  title = `${SITE_CONFIG.name} — ${SITE_CONFIG.tagline}`,
  description = SITE_CONFIG.description,
  canonical = `${SITE_CONFIG.origin}${Astro.url.pathname}`,
  noindex = false,
  jsonLd,
} = Astro.props

const beaconToken = import.meta.env.PUBLIC_CF_BEACON_TOKEN
const cfAnalyticsEnabled = import.meta.env.PUBLIC_ENABLE_CF_ANALYTICS === 'true'
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    {noindex && <meta name="robots" content="noindex" />}
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={`${SITE_CONFIG.name} — oriz`} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@chirag127" />
    <meta name="theme-color" content="#ECEEF1" />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,500;12..96,600;12..96,700;12..96,800&family=Spline+Sans:wght@400;500;600&family=Martian+Mono:wght@400;500;600&display=swap"
    />

    <JsonLd extra={jsonLd} pageUrl={canonical} />

    {
      cfAnalyticsEnabled && beaconToken && (
        <script
          is:inline
          defer
          src="https://static.cloudflareinsights.com/beacon.min.js"
          data-cf-beacon={`{"token": "${beaconToken}"}`}
        />
      )
    }
  </head>
  <body>
    <FamilyChrome slug="oriz-portfolio-lab" title="Portfolio Lab" accent="#3D3AE0" />
    <a href="#main" class="skip-link">Skip to content</a>
    <Header />
    <Sidebar>
      <AppSidebarContent />
    </Sidebar>
    <main id="main">
      <div class="oriz-shell">
        <div class="oriz-main"><slot /></div>
      </div>
    </main>
    <Footer />
    <BottomBar actions={bottomBarActions} />
    <ConsentBanner />
  </body>
</html>
